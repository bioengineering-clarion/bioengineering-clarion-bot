class Handler {
	constructor(paprams) {
        this.ReplyMarkup = paprams.ReplyMarkup;
        this.status_vocab = paprams.status_vocab
        this.config = paprams.config
    }
}

module.exports = Handler